Resume job search <run-id>

Đọc AGENTS.md. Resume bằng config snapshot/checkpoint của run này. Không bắt
đầu lại run, không bỏ qua nguồn đang chờ. Chỉ tiếp tục thao tác cần đăng nhập
sau khi người dùng đã hoàn tất và kiểm tra session bằng công cụ browser thực
tế. Không có browser thì dùng nhập JD thủ công và báo blocked. Tiếp tục query,
candidate và báo cáo còn dở trong budget của run; không tạo CV.
