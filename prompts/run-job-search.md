Run job search

Đọc AGENTS.md và config/search-config.yaml mới nhất. Validate config, tạo run,
dùng code quản lý state/dedup/filter/output. Thực hiện query theo checkpoint
bằng web search thực sự có sẵn; dùng public adapters và nhập observation có
bằng chứng. Ghi candidate ngay, không coi snippet là JD đầy đủ. Checkpoint
nguồn bị chặn, hỗ trợ takeover khi có công cụ và người dùng hoàn tất, tiếp tục
nguồn độc lập. Báo coverage và xuất manifest/handoff; không tạo CV hay nộp hồ sơ.
