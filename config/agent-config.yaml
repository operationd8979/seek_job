# Shared model for the local UI's job search and CV generation agents.
# Changes are read when the next operation starts; existing operations keep their model.
model: gpt-5.6-sol
reasoning_effort: medium
